# Forward zone configuration for Unbound
# This file is intentionally empty to disable forwarding
# Unbound will use root hints for recursive resolution

# To enable forwarding to specific DNS servers, uncomment and modify the lines below:
# forward-zone:
#     name: "."
#     forward-tls-upstream: yes
#     forward-addr: 1.1.1.1@853#cloudflare-dns.com
#     forward-addr: 1.0.0.1@853#cloudflare-dns.com 