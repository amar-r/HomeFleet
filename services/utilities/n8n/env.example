# n8n Environment Configuration
# NOTE: This file is for reference only. 
# Environment variables are now loaded from the root .env file for Makefile integration.
# Copy these variables to the root .env file if needed.

# Basic Authentication (required for security)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme

# Server Configuration
N8N_PORT=5678
N8N_HOST=localhost
WEBHOOK_URL=http://localhost:5678

# Timezone
GENERIC_TIMEZONE=America/New_York

# Optional: Database (defaults to SQLite)
# N8N_DATABASE_TYPE=postgresdb
# N8N_DATABASE_POSTGRESDB_HOST=postgres
# N8N_DATABASE_POSTGRESDB_PORT=5432
# N8N_DATABASE_POSTGRESDB_DATABASE=n8n
# N8N_DATABASE_POSTGRESDB_USER=n8n
# N8N_DATABASE_POSTGRESDB_PASSWORD=n8n_password

# Optional: Redis for queue management
# N8N_REDIS_HOST=redis
# N8N_REDIS_PORT=6379
# N8N_REDIS_PASSWORD=redis_password 